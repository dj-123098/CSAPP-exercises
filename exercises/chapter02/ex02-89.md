#### A
(float) x == (float) dx
1

#### B
dx - dy == (double) (x-y)
overflow

#### C
(dx + dy) + dz == dx + (dy + dz)
precision

#### D
(dx * dy) * dz == dx * (dy * dz)
0

#### E
dx / dx == dz / dz
dx = 0
